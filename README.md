# ML-PROJECT1
Machine learning project on specific dataset to find the predictions and best model.
Step-wise model building 
EDA → preprocessing → feature engineering → train/test split → 5 models → evaluation → hyperparameter tuning → comparison → predictions → error analysis
